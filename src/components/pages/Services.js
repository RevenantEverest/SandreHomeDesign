import React, { Component } from 'react';

class Services extends Component {

    constructor(props) {
        super(props);
        this.state = {};
    }

    render() {
        return(
            <div id="Services" className={`${this.props.className}`}>
            </div>
        );
    }
};

export default Services;